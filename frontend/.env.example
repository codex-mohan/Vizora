# With portless (recommended for local dev):
NEXT_PUBLIC_API_URL=https://api.localhost

# Without portless (direct ports):
# NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# Optional: set this to a MapLibre-compatible Mappls/MapmyIndia style URL
# for India-grade roads, streets, POIs, and traffic-aware basemap details.
# If omitted, the analytics map falls back to inline OSM raster tiles.
NEXT_PUBLIC_MAP_STYLE_URL=
